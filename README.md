## IBM JIRA Issue Tracking System

```bash
mvn clean install
```

```bash
mvn spring-boot:run
```

Alternatively, run the following command-line argument directly:

```bash
mvn clean install spring-boot:run 
```